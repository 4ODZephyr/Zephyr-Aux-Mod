# Unit Patch Addon

Adds a custom Zephyr unit patch (insignia) that can be applied to uniforms.

## Usage

The patch can be applied to a unit's uniform in the Virtual Arsenal or via script:

```sqf
[_unit, "Zephyr_Patch"] call BIS_fnc_setUnitInsignia;
```

## Requirements

- Zephyr Core addon
